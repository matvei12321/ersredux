TST_nature_taming_category = {

	nature_taming_decision = {
    state_target = yes
    target_root_trigger = {
        has_completed_focus = TST_comm_ec_11
    }
    target_array = TST.core_states
    target_trigger = {
        FROM = {
			AND = {
				ROOT = {
					num_of_civilian_factories_available_for_projects > 1
				}
				is_owned_by = ROOT
				OR = {
					AND = {
						OR = {
							has_state_category = wasteland
							has_state_category = enclave
							has_state_category = tiny_island
						}
						state_population > 70000
					}
					AND = {
						OR = {
							has_state_category = pastoral
							has_state_category = small_island
						}
						state_population > 130000
						infrastructure > 0
					}
					AND = {
						has_state_category = rural
						state_population > 250000
						infrastructure > 1
					}
					AND = {
						has_state_category = town
						state_population > 600000
						infrastructure > 2
					}
					AND = {
						has_state_category = large_town
						state_population > 900000
						infrastructure > 3
					}
					AND = {
						has_state_category = city
						state_population > 1300000
						infrastructure > 3
					}
					AND = {
						has_state_category = large_city
						state_population > 2600000
						infrastructure > 4
					}
					AND = {
						has_state_category = metropolis
						state_population > 5000000
						infrastructure > 4
					}
				}
			}
        }
    }
    on_map_mode = map_and_decisions_view
    icon = GFX_decision_modern_city
    cost = 25
	days_remove = 50
	complete_effect = {
	
	}
	modifier = {
        civilian_factory_use = 2
    }
    remove_effect = {
        FROM = {
            improve_state_category = yes
        }
    }
}

}